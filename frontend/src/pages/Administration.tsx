export default function Administration() {
  return (
    <div>
      <h1 className="mb-1 text-xl font-semibold text-gray-900">Administration</h1>
      <p className="mb-6 text-sm text-gray-500">Configuration dynamique des formulaires, paramètres système et gabarit du PPB (Super Admin).</p>
      <div className="rounded-lg border border-dashed border-gray-300 bg-white p-10 text-center text-sm text-gray-400">
        Écran à implémenter — structure de route déjà branchée et protégée par rôle.
      </div>
    </div>
  );
}
